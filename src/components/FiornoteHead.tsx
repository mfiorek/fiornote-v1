import React from "react";
import Head from "next/head";

const FiornoteHead = () => {
  return (
    <Head>
      <title>fiornote</title>
      <meta name="description" content="fiornote - Simple note app" />
    </Head>
  );
};

export default FiornoteHead;
